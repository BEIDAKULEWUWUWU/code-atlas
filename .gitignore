# 扫描输出。README 里那条 `npm run scan ../my-project --out my-project.json` 默认就落在仓库根目录，
# 不挡住的话第一次用就会把别人的项目 json 提交进来。
my-project.json
*.scan.json

# 本机配置。`.claude/settings.local.json` 里有本机用户名路径，不该跟着仓库走。
.claude/settings.local.json

node_modules/
.DS_Store
Thumbs.db
